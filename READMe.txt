The solution (.sln file) is inside the KironTest folder.
When running the solution in Visual Studio, please set the Kiron.Test.Web.API as the Startup Project.
All database scripts that includes all tables and stored procedures are in the SQL Database Scripts.
The newly backed-up database is in the Database Backup folder. The Name of the database is KironTest.